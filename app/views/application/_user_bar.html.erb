<% if user = current_user %>
  <%= render 'admin_bar' if user.admin? %>
  <%= link_to user.email, edit_user_path(user) %> |
   <%= link_to "Logout", logout_path %>
<% else %>
  <%= link_to "Login", login_path %>
<% end %>
