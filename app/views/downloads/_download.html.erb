<%= link_to download.file_url.to_s do %>
  <article class="well">
  	<span class="date">Vor <%= distance_of_time_in_words_gebeugt(download.created_at, Time.now) %></span>
    <div class="icon"><%= image_tag Icon.for_filename(download.file_url.to_s) %></div>
    <div><h2><%= download.name %></h2></div>
    <%=(@is_allowed_to_upload) ? link_to("X", download_path(download), :method => :delete, confirm: "#{download.name} wirklich löschen?", class: "btn btn-danger btn-small") : "" %>
    
  </article>
<% end %>
